import appConfig from './app.config';
import emailConfig from './email.config';
import httpConfig from './http.config';
import rateConfig from './rate.config';

export default [appConfig, emailConfig, httpConfig, rateConfig];
